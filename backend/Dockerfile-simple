FROM eclipse-temurin:17-jdk-alpine

WORKDIR /app

# Create a simple Spring Boot application
COPY src/main/resources/application.yml application.yml

# Create a simple JAR file placeholder
RUN echo "Creating placeholder JAR..." && \
    mkdir -p target && \
    echo "Placeholder JAR" > target/virtual-tryon-platform-1.0.0.jar

# Expose port
EXPOSE 8080

# For now, just keep the container running
CMD ["sh", "-c", "echo 'Backend service placeholder - implement compilation later' && sleep infinity"]